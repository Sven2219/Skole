import { Dimensions } from "react-native";

export const { width, height } = Dimensions.get("window");

export const ICON_SIZE = 25;
export const TEXT_INPUT_WIDTH = width * 0.9;
export const TEXT_INPUT_HEIGHT = 50;

export const BUBBLE_WIDTH = width / 2.2;
export const BUBBLE_HEIGHT = 50;


export const DETAILS_CONTAINER_WIDTH = width * 0.9;
export const DETAILS_CONTAINER_HEIGHT = 80;
export const IMAGE_SIZE = 100;

export const MENU_WIDTH = 100;
export const MENU_HEIGHT = 40;